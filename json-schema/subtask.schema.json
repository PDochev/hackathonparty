{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "placeholder",
    "title": "Subtask",
    "description": "A description of a task that is either not done or done",
    "type": "object",
    "properties": {
        "task_id": {
            "type": "string"
        },
        "title": {
            "type": "string"
        },
        "description": {
            "type": "string"
        },
        "instructions": {
            "type": "string"
        },
        "created_at": {
            "type": "string",
            "format": "date-time"
        },
        "updated_at": {
            "type": "string",
            "format": "date-time"
        }
    },
    "required": [
        "user_id",
        "title",
        "description",
        "instructions",
        "created_at",
        "updated_at"
    ]
}